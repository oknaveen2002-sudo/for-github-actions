package com.example.demo;

import static org.junit.Assert.assertEquals;
import org.junit.Test;

/**
 * A trivial test so the GitHub Actions pipeline has something to run
 * during `mvn test`. Replace with real unit tests as your app grows.
 */
public class SanityTest {

    @Test
    public void additionWorks() {
        assertEquals(4, 2 + 2);
    }
}
